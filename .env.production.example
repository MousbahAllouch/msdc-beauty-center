# Production Environment Variables for Railway
# Copy these to Railway Dashboard → Variables tab

# Node Environment
NODE_ENV=production

# MongoDB Connection (from MongoDB Atlas)
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/beauty_center?retryWrites=true&w=majority

# Server Port (Railway will auto-assign, but good to have)
PORT=5000

# Frontend URL (your Railway app URL)
FRONTEND_URL=https://your-app-name.railway.app
